import { ShowCustromizeLogoBtnDirective } from './show-custromize-logo-btn.directive';

describe('ShowCustromizeLogoBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowCustromizeLogoBtnDirective();
    expect(directive).toBeTruthy();
  });
});

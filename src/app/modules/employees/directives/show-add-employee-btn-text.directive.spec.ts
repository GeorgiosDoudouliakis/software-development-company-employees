import { ShowAddEmployeeBtnTextDirective } from './show-add-employee-btn-text.directive';

describe('ShowAddEmployeeBtnTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowAddEmployeeBtnTextDirective();
    expect(directive).toBeTruthy();
  });
});

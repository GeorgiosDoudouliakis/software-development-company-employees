import { ShowCustomizeBtnDirective } from './show-customize-btn.directive';

describe('ShowCustomizeBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowCustomizeBtnDirective();
    expect(directive).toBeTruthy();
  });
});
